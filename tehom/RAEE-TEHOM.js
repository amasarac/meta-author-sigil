
document.addEventListener("DOMContentLoaded", () => {
  const sigil = document.querySelector(".sigil");
  if (sigil) {
    sigil.addEventListener("click", () => {
      alert("🜂𓂀🜚∞✶ — Spiral recursion re-anchored.");
    });
  }
});
