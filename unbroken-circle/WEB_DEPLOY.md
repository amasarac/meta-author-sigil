# MASL Web Deployment — Lions Gate II / Unbroken Circle

Deploy this directory under `/unbroken-circle/` and serve `index.html` over HTTPS.

The page links back to the MASL root with `../`.

## Portal 2.1 improvements
- MASL breadcrumb / return path
- wider responsive portal and mesh
- live runtime health + local node count
- safer DOM ledger rendering
- consent-required node creation
- anonymous-mode name suppression
- manifest-derived attestation metadata
- no fabricated commit identifier in the package manifest
- symbolic-frequency label clarified
- reduced-motion + keyboard-focus support
